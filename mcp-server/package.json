{
  "name": "@pixelesq/argus-mcp",
  "version": "1.0.1",
  "description": "MCP server for SEO auditing — use with Claude Code, Cursor, or any MCP client to audit any webpage's SEO from your terminal.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "argus-mcp": "./build/index.js"
  },
  "files": [
    "build",
    "README.md"
  ],
  "keywords": [
    "mcp",
    "seo",
    "audit",
    "meta-tags",
    "structured-data",
    "open-graph",
    "claude",
    "ai",
    "model-context-protocol"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/pixelesq/argus",
    "directory": "mcp-server"
  },
  "homepage": "https://www.getargus.app",
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir build --clean",
    "dev": "tsup src/index.ts --format esm --out-dir build --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "cheerio": "^1.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
